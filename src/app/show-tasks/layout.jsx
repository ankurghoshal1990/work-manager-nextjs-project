export const metadata = {
    title: "All Tasks:  Work Manager"
}

const ShowTasksLayout = ({children}) => {
    return (
      <div>
          {children}
      </div>
    )
}
  
export default ShowTasksLayout