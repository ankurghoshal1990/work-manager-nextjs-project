export const metadata = {
    title: "Add Task:  Work Manager"
}

const AddTaskLayout = ({children}) => {
    return (
      <div>
          {children}
      </div>
    )
}
  
export default AddTaskLayout